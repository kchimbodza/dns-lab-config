version: "3"
services:
  bind9:
    image: internetsystemsconsortium/bind9:9.18
    container_name: dns-lab
    ports:
      - "53:53/udp"
      - "53:53/tcp"
    volumes:
      - ./config/dns-server.conf:/etc/bind/dns-server.conf
      - ./config/zones:/etc/bind/zones
    restart: unless-stopped
